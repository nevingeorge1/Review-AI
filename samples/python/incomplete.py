# Incomplete / syntax error sample for parser resilience testing

def unfinished_function(arg1, arg2):
    # Syntax error: unclosed parenthesis and missing colon
    if arg1 > 10
        return arg1 *
